---
# tasks file for roles/app

- name: Run App container
  docker_container:
    name: simple-api
    image: bigboypro/tp-devops-simple-api:latest
    pull: true
    networks:
      - name: app-network
    env:
      DATABASE_HOST: database
      DATABASE_PASSWORD: pwd
    state: started
    restart_policy: unless-stopped
